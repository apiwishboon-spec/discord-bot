DISCORD_TOKEN=PASTE_DISCORD_TOKEN_HERE
CLIENT_ID=YOUR_APPLICATION_CLIENT_ID
# Optional: register commands to one guild instantly while testing
GUILD_ID=
# Optional: moderation logs channel ID (numbers only)
LOG_CHANNEL_ID=
# Optional: welcome channel ID for auto greet on member join
WELCOME_CHANNEL_ID=
# Set true to enable member join greeting (requires Server Members Intent)
ENABLE_MEMBER_EVENTS=false
